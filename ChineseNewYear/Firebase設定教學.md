# 🔥 Firebase 設定教學

## 📋 完整步驟指南

### 步驟 1：建立 Firebase 專案

1. **開啟 Firebase Console**
   - 網址：https://console.firebase.google.com/
   - 使用您的 Google 帳號登入

2. **建立新專案**
   - 點擊「新增專案」按鈕
   - 專案名稱：`chinese-new-year-orders`
   - 點擊「繼續」
   - Google Analytics：選擇「暫時不要」
   - 點擊「建立專案」
   - 等待專案建立完成（約 30 秒）

---

### 步驟 2：設定 Firestore Database

1. **建立資料庫**
   - 在左側選單找到並點擊「Firestore Database」
   - 點擊「建立資料庫」按鈕

2. **選擇安全規則**
   - 選擇「以測試模式啟動」
   - ⚠️ 注意：測試模式會在 30 天後過期，屆時需要更新規則

3. **選擇位置**
   - 建議選擇：`asia-east1 (Taiwan)` 或 `asia-east2 (Hong Kong)`
   - 點擊「啟用」
   - 等待資料庫建立完成

---

### 步驟 3：取得 Firebase 配置

1. **註冊 Web 應用程式**
   - 點擊專案概覽旁的齒輪圖示 ⚙️
   - 選擇「專案設定」
   - 向下捲動到「您的應用程式」區域
   - 點擊「</>」圖示（Web 平台）

2. **設定應用程式**
   - 應用程式暱稱：`年菜訂購系統`
   - **不要**勾選「同時設定 Firebase Hosting」
   - 點擊「註冊應用程式」

3. **複製配置代碼**
   - 您會看到類似這樣的代碼：
   ```javascript
   const firebaseConfig = {
     apiKey: "AIzaSyB...",
     authDomain: "your-project.firebaseapp.com",
     projectId: "your-project",
     storageBucket: "your-project.appspot.com",
     messagingSenderId: "123456789",
     appId: "1:123456789:web:..."
   };
   ```
   - **複製整個 firebaseConfig 物件的內容**

---

### 步驟 4：更新配置檔案

1. **編輯 firebase-config.js**
   - 開啟專案中的 `firebase-config.js` 檔案
   - 找到這段代碼：
   ```javascript
   const firebaseConfig = {
     apiKey: "YOUR_API_KEY",
     authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
     projectId: "YOUR_PROJECT_ID",
     storageBucket: "YOUR_PROJECT_ID.appspot.com",
     messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
     appId: "YOUR_APP_ID"
   };
   ```

2. **替換配置**
   - 將您從 Firebase Console 複製的值貼上
   - 例如：
   ```javascript
   const firebaseConfig = {
     apiKey: "AIzaSyB1234567890abcdefghijk",
     authDomain: "chinese-new-year-orders.firebaseapp.com",
     projectId: "chinese-new-year-orders",
     storageBucket: "chinese-new-year-orders.appspot.com",
     messagingSenderId: "123456789012",
     appId: "1:123456789012:web:abcdef123456"
   };
   ```

3. **儲存檔案**
   - 按 Ctrl + S (Windows) 或 Cmd + S (Mac)

---

### 步驟 5：測試連接

1. **開啟網頁**
   - 用瀏覽器打開 `index.html`
   - 按 F12 開啟開發者工具
   - 切換到「Console」標籤

2. **檢查訊息**
   - 如果成功，您會看到：
     ```
     ✅ Firebase 已成功連接！
     📊 資料將同步到雲端資料庫
     ```
   
   - 如果失敗，您會看到：
     ```
     ⚠️ Firebase 配置尚未設定
     📝 請編輯 firebase-config.js 填入您的 Firebase 配置
     ```

---

### 步驟 6：設定安全規則（重要！）

測試模式 30 天後會過期，建議設定適當的安全規則：

1. **進入 Firestore 規則**
   - Firebase Console → Firestore Database → 規則

2. **更新規則**
   - 將規則改為：
   ```javascript
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       // 允許所有人讀寫訂單（適合小型內部使用）
       match /orders/{orderId} {
         allow read, write: if true;
       }
       
       // 允許所有人讀寫設定
       match /settings/{setting} {
         allow read, write: if true;
       }
     }
   }
   ```

3. **發布規則**
   - 點擊「發布」按鈕

⚠️ **注意**：上述規則允許任何人讀寫，僅適合內部使用。如果需要更嚴格的安全性，請參考 Firebase 安全規則文件。

---

## ✅ 完成！

現在您的訂購系統已經連接到 Firebase：

- ✅ 所有訂單會自動同步到雲端
- ✅ 多人可以同時新增訂單
- ✅ 資料不會遺失
- ✅ 任何裝置都能查看最新資料

## 🎯 使用方式

1. **新增訂單**：會自動同步到 Firebase
2. **查看訂單**：打開網頁會自動載入 Firebase 資料
3. **刪除訂單**：會同時從本地和 Firebase 刪除
4. **菜品管理**：也會同步到 Firebase

## 🔍 驗證資料

在 Firebase Console 的 Firestore Database 中，您可以看到：
- `orders` 集合：所有訂單資料
- `settings` 集合：菜品設定

## ❓ 常見問題

**Q: 如果沒有網路怎麼辦？**
A: 系統會自動使用本地 localStorage，等有網路時再同步。

**Q: 多人同時使用會衝突嗎？**
A: 不會，每筆訂單都有唯一 ID，可以安全地多人使用。

**Q: 資料會遺失嗎？**
A: 不會，資料同時儲存在 Firebase 和本地，雙重保障。

**Q: 免費額度夠用嗎？**
A: Firebase 免費額度非常充足，每天可處理數萬筆資料。

## 📞 需要協助？

如果遇到問題，請檢查：
1. Firebase 配置是否正確填寫
2. 瀏覽器 Console 的錯誤訊息
3. Firebase Console 的安全規則設定
